# Flavors

This directory contains the distro flavors managed inside the ARKYN Linux repo.

Each flavor should live in its own subdirectory and keep only flavor-specific files here. Shared base logic should stay at the repo root or in common tooling.

Current flavors:
- NOVA
- ORION
- SENTINEL
- ATLAS