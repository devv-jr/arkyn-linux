# SDDM config

This folder contains the SDDM defaults for ATLAS.

Files:
- `sddm.conf` for the login manager theme and display settings

The installer copies this file to `/etc/sddm.conf.d/90-atlas.conf`.